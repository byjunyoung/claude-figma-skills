# {Tool} as the chat source / as the calendar

<!-- Keep the sections this tool answers. Every call should be one you ran; mark one you have
     not with (unverified). -->

For a `sources.chat_type` / `sources.calendar_type` of `{type}`.

## A thread from a link

```
{call that returns the root and every reply, given a permalink}
```

{How a link to a reply resolves to the root. What images and files come back as.}

## A channel on a day

```
{call that returns a channel's messages between two times}
```

## Messages about me on a day

```
{search call for from:me and mentions of me, bounded to a date}
```

{The response cap, and how to page.}

## My notes channel

{How a message posted by an app is told apart from one I typed — an app id, a bot flag.}

## Permalinks and names

```
{call that returns a message's permalink}
{call that returns a user's display name}
```

## A day's events

```
{call that returns events between two times in local time, with attendees}
```

{Which entries to drop.}

## Things that bite

- {one line per thing, each because it happened}
